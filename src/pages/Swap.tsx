import NavBar from '../components/NavBar';
import SwapForm from '../components/SwapForm';

const Swap = () => {
    return (
        <>
            <NavBar />
            <SwapForm />
        </>
    );
};

export default Swap;
